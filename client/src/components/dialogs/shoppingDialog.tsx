import * as React from "react";

import * as styled from "styled-components";

import { Store } from '../../lib/App/store';

export class ShoppingDialog extends React.Component <{}, {}> {
    render(){
        return(
            null
        )
    }
}
